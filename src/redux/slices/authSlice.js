// NOTE: Redux is configured here but auth state is managed by AuthContext (src/context/AuthContext.jsx).
// The authSlice was empty — Redux is intentionally unused. 
// You can safely remove this entire redux/ folder if no Redux state is needed in future.
